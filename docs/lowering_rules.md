# Lowering rules

Here, we list all the current lowering rules. These rules are internally used by `juju`'s lowering interpreter, and this list reflects the coverage of lowering over JAX's primitives.

::: juju.rules
    options:
        members: true
        filters: 
            - "!Ruleset"